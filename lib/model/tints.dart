import 'package:flutter/material.dart';
import 'package:flutter_photo/service/tint.dart';

class Tints {
  List<Tint> list() {
    return [
      Tint(color: Colors.red),
      Tint(color: Colors.deepOrange),
      Tint(color: Colors.pink),
      Tint(color: Colors.orange),
      Tint(color: Colors.yellow),
      Tint(color: Colors.brown),
      Tint(color: Colors.green),
      Tint(color: Colors.teal),
      Tint(color: Colors.blue),
      Tint(color: Colors.indigo),
      Tint(color: Colors.purple),
      Tint(color: Colors.purple.shade900),
      Tint(color: Colors.grey),
    ];
  }
}
